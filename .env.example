# Path to the SQLite database file
# Defaults to ./tickets.db if not set
DB_PATH=./tickets.db

# JWT signing secret — use a long random string in production
JWT_SECRET=your-super-secret-key-change-this

# Port (optional, defaults to 8080)
PORT=8080
